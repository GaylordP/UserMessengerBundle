{% if app.user != user %}
    {{ 'action.user.message.send'|trans({}, 'user_messenger')|button_link(
        path('user_message_member',
            {
                'slug': user.slug,
            }
        ),
        'blue',
        include('@UserMessenger/include/_ico.html.twig', with_context = false),
        true,
        {
            'class': 'btn-sm',
        }
    ) }}
{% endif %}