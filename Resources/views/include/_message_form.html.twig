{{ form_start(form) }}
    <div>
        {{ form_row(form.message) }}
        {{ 'action.user.message.join.user_media'|trans({}, 'user_messenger')|button('secondary', 'fas fa-upload', false, {
            'class': 'btn-sm user-messenger-toggle-user-media',
        }) }}
        {{ form_row(form.upload) }}
        {{ form_row(form.userMedias) }}
    </div>
    {{ 'action.user.message.send_short'|trans({}, 'user_messenger')|button(
        '_new_',
        include('@UserMessenger/include/_ico.html.twig', with_context = false),
        false,
        {
            'type': 'submit',
        }
    ) }}
{{ form_end(form) }}